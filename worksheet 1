Q1) Write a c program to find the sum of two numbers.

#include <stdio.h>
int main()
{
int a,b,sum;
printf("Enter the first number:");
scanf("%d",&a);
printf("Enter the second number:");
scanf("%d",&b):
sum=a+b;
printf("The sum of two numbers is: %d",sum);
return 0;
}

Q2) Write a c program to check whether a number is even or odd.

#include <stdio.h>
int main()
{
int n;
printf("Enter the number you want to check");
scanf("%d",&n);
if(n%2==0)
{
printf("%d is an even number",n);
}
else
{
printf("%d is an odd number",n);
}
return 0;
}

Q3) Write a c program to print the first 10 natural numbers.

#include <stdio.h>
int main()
{
int i;
printf("The first 10 natural numbers are:\n");
for(i=1;i<=10;i++)
{
printf("%d",i);
}
return 0;
}

Q4) Write a program to input marks of 5 subjects and calculate the total and average.

#include <stdio.h>
int main()
{
int total, m, p, h, e, c;
float avg;
